import {db} from "../../../Firebase";
import {AUTH_USER, SHOW_SNACKBAR} from "../../type";
import * as firebase from "firebase";
import {getDateWithHourMnSeconds} from "../../calcul/getDate";


/**
 * This function will add a post on the google map
 * @param values        {object} :  contain login's and password's values
 * @param auth          {object} :  user's data
 * @param saveInUser
 * @returns             {function(...[*]=)}
 */
export const createTrader = (values) => async dispatch => {

};


